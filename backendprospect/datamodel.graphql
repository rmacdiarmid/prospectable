type User {
  id: ID! @id
  name: String!
  email: String!
  phone: Int!
}

type Item {
  id: ID! @id
  title: String!
  description: String!
  image: String
  largeImage: String
  amount: Int!
  # createAt: DateTime!
  # updatedAt: DateTime!
  # user: User!
}