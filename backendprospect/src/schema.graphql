# import * from './generated/prisma.graphql'

type Mutation {
    createItem(title: String, description: String, amount: Int, image: String, largeImage: String): Item!
}

type Query {
    items: [Item]!
}